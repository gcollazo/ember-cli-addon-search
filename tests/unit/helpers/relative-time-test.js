import {
  relativeTime
} from 'ember-cli-addon-search/helpers/relative-time';

module('RelativeTimeHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = relativeTime(42);
  ok(result);
});
