import filterByQuery from '../../../utils/filter-by-query';
import { module, test } from 'qunit';

module('Unit | Utility | filter by query');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = filterByQuery();
  assert.ok(result);
});
